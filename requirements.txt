# Core App Framework
streamlit

# Computer Vision
# Use the "headless" version for servers as it's smaller and avoids GUI dependencies
opencv-python-headless

# AI Model Library
ultralytics

# Numerical Processing
numpy

# Core ML Libraries (CPU versions)
# Tell pip to look in the PyTorch index as an EXTRA source for these specific packages
--find-links https://download.pytorch.org/whl/cpu
torch
torchvision
torchaudio
